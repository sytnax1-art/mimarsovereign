# MimarSovereign

This repository is published using GitHub Pages:

https://sytnax1-art.github.io/mimarsovereign

The file `index.html` was copied to the default branch (main) from the existing `gh-pages` branch so the site can be served from the repository root. If you prefer another Pages source (for example `gh-pages` or `main/docs`), update the Pages settings in the repository settings.
